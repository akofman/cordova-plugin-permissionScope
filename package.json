{
  "name": "cordova-plugin-permissionScope",
  "version": "2.0.2",
  "description": "Cordova plugin description",
  "homepage": "https://github.com/akofman/cordova-plugin-permissionScope",
  "author": {
    "name": "Alexis Kofman"
  },
  "license": "Apache-2.0",
  "cordova": {
    "id": "cordova-plugin-permissionScope",
    "platforms": [
      "ios"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/akofman/cordova-plugin-permissionScope.git"
  },
  "keywords": [
    "cordova",
    "ios",
    "ecosystem:cordova",
    "cordova-ios",
    "cordova:plugin",
    "permission",
    "permissionScope"
  ],
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-eslint": "^6.0.2",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.6.0",
    "cordova": "^6.1.1",
    "cordova-paramedic": "^0.5.0",
    "eslint": "^2.6.0",
    "rimraf": "^2.3.4"
  },
  "scripts": {
    "build": "babel src/www -d .",
    "clean": "cd tests/app && rimraf platforms plugins",
    "test": "eslint src/permissionScope.js tests/tests.js",
    "install": "npm run build && cd tests/app && cordova platform rm ios && cordova platform add ios",
    "deploy": "cd tests/app && cordova run --device"
  }
}
